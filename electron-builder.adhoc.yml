extends: electron-builder.yml
mac:
  # Ad-hoc sign so Apple Silicon doesn't report the app as "damaged" (identity: null
  # skips signing entirely). Still requires user approval — not notarized.
  identity: '-'
  hardenedRuntime: false
  notarize: false
