name: Release

on:
  push:
    tags:
      - v*
  workflow_dispatch:
    inputs:
      tag:
        description: Git tag to release (e.g. v1.1.3)
        required: true
        type: string

permissions:
  contents: write

jobs:
  release-mac:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && format('refs/tags/{0}', inputs.tag) || github.ref }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Build and publish macOS release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_FOR_PULL_REQUEST: true
        run: |
          # Empty CSC_LINK from the job env breaks ad-hoc signing (identity: "-").
          unset CSC_LINK
          bun run release:mac

      - name: Verify code signatures
        run: |
          codesign --verify --deep --strict --verbose=2 dist/mac-arm64/Sediment.app
          codesign --verify --deep --strict --verbose=2 dist/mac/Sediment.app
