# Public Convex production deployment URLs (safe to commit — not secrets).
# Used by `electron-vite build` / release CI. Local `bunx convex dev` still
# writes .env.local for the personal development deployment.
VITE_CONVEX_URL=https://bright-lark-824.convex.cloud
VITE_CONVEX_SITE_URL=https://bright-lark-824.convex.site
