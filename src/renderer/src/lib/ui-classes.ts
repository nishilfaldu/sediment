/** Secondary action button — Add note, empty states, etc. */
export const secondaryButtonClass =
  'border border-ui bg-card px-3 py-1.5 text-xs font-medium text-secondary transition-colors hover:border-primary hover:text-primary'

/** Specimen tag on card thumbnails (semi-transparent over imagery). */
export const specimenTagOverlayClass =
  'border border-ui bg-card/95 px-1.5 py-0.5 font-mono text-[9px] font-medium uppercase tracking-[0.14em] text-moss'

/** Specimen tag on solid backgrounds (search results, cards without thumbnails). */
export const specimenTagClass =
  'border border-ui bg-card px-1.5 py-0.5 font-mono text-[9px] font-medium uppercase tracking-[0.14em] text-moss'
